
export const execute = (name, data, delayed) => {
    /*require([
        'generic/AmplitudeEvent',
        'modules/statistics/EventQueue'
    ], (AmplitudeEvent, EventQueue) => {
        if (delayed) {
            EventQueue.push(data, name);
        } else {
            new AmplitudeEvent(data).send(name);
        }
    })*/
}; 
